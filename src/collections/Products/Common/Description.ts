import { Field } from "payload";

const Description: Field = {
  name: "description",
  type: "richText",
  // required: true,
  label: {
    en: "Description",
    vi: "Mô Tả",
  },
};

export default Description;
