import { Field } from "payload";

const EnableVariations: Field = {
  name: "enableVariations",
  type: "checkbox",
  defaultValue: false,
  label: {
    en: "Variations?",
    vi: "Kích Hoạt Biến Thể?",
  },
};

export default EnableVariations;
